### A repository for learning and upgading from javascrip to type safe javascript - aka TypeScript

startdate : 30-june-2026

old-knowledge :- upgraded my knowledge to code from a `monolith` app to a more `modular` style app where each module has its own folder and folders contain `module.route.js`, `module.controller.js`,`module.model.js`and `module.service.js` using this flow completed a project named `Stock Lane` backend a multi tenant inventory management system using `nodejs` and `express` for server.